<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:windows="clr-namespace:Veyra.Desktop.ViewModels.Windows"
        mc:Ignorable="d"
        x:Class="Veyra.Desktop.Views.Windows.CreateRepositoryWindow"
        x:DataType="windows:CreateRepositoryWindowViewModel"
        Width="900"
        Height="620"
        MinWidth="760"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        Background="#101010"
        Foreground="#EEEEEE"
        Title="Создание репозитория">

    <DockPanel Margin="20">
        <Border DockPanel.Dock="Top" Background="#141414" CornerRadius="10" Padding="16" Margin="0,0,0,16">
            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Title}" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding StepInfo}" Opacity="0.7" Margin="0,6,0,0"/>
                <TextBlock Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                           Text="{Binding ErrorMessage}" Foreground="#F87171"
                           MaxWidth="360" TextWrapping="Wrap"
                           IsVisible="{Binding HasErrorMessage}"/>
            </Grid>
        </Border>

        <Grid RowDefinitions="*,Auto">
            <Border Grid.Row="0" Background="#141414" CornerRadius="10" Padding="16">
                <Grid>
                    <StackPanel IsVisible="{Binding IsStepRepository}" Spacing="12">
                        <TextBlock Text="Укажите директорию и имя репозитория" FontSize="15" FontWeight="SemiBold"/>

                        <TextBlock Text="Имя репозитория" Opacity="0.8"/>
                        <TextBox Text="{Binding RepositoryName, Mode=TwoWay}" Watermark="Например MyProject"/>

                        <TextBlock Text="Описание" Opacity="0.8"/>
                        <TextBox Text="{Binding Description, Mode=TwoWay}" AcceptsReturn="True" Height="80"/>

                        <TextBlock Text="Директория" Opacity="0.8"/>
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                            <TextBox Grid.Column="0" Text="{Binding DirectoryPath, Mode=TwoWay}"/>
                            <Button Grid.Column="1" Content="Обзор" Command="{Binding BrowseDirectoryCommand}"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel IsVisible="{Binding IsStepFormats}" Spacing="12">
                        <TextBlock Text="Выберите форматы файлов для отслеживания" FontSize="15" FontWeight="SemiBold"/>

                        <ScrollViewer VerticalScrollBarVisibility="Auto" Height="360">
                            <ItemsControl ItemsSource="{Binding Formats}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="windows:RepositoryFormatOptionViewModel">
                                        <ToggleButton Margin="4" Padding="10,5"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}">
                                            <TextBlock Text="{Binding Format}"/>
                                        </ToggleButton>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                            <TextBox Grid.Column="0" Text="{Binding CustomFormat, Mode=TwoWay}" Watermark="Добавить формат например .sql"/>
                            <Button Grid.Column="1" Content="Добавить" Command="{Binding AddCustomFormatCommand}"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel IsVisible="{Binding IsStepScan}" Spacing="14">
                        <TextBlock Text="Сканирование и сохранение" FontSize="15" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding ProgressMessage}" Opacity="0.85"/>

                        <ProgressBar Minimum="0" Maximum="100" Value="{Binding ProgressPercent}" Height="16" IsIndeterminate="{Binding IsProgressIndeterminate}"/>

                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                            <TextBlock Grid.Column="0" Text="Прогресс" Opacity="0.8"/>
                            <TextBlock Grid.Column="1" Text="{Binding ProgressPercent, StringFormat='{}{0}%'}"/>
                        </Grid>

                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                            <TextBlock Grid.Column="0" Text="Файлов" Opacity="0.8"/>
                            <TextBlock Grid.Column="1" Text="{Binding FilesProgressLabel}"/>
                        </Grid>

                        <TextBlock Text="Окно можно закрыть после завершения процесса"
                                   Opacity="0.6" FontSize="12"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,16,0,0">
                <Button Content="Назад" Width="110" Command="{Binding BackCommand}"/>
                <Button Content="Отмена" Width="110" Command="{Binding CancelCommand}"/>
                <Button Content="{Binding NextButtonText}" Width="140" Command="{Binding NextCommand}"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>

